[package]
name = "realtime_server"
version = "1.0.0"
edition = "2024"
description = "Standalone package to read a .ACMI file and host a single client Tacview Real Time Telemetry stream"

[dependencies]
clap = { version = "4.6.4", features = ["derive"] }
ctrlc = "3.5.2"
zip = "8.6.0"
